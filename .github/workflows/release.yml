name: Release

on:
  push:
    branches: [main]

permissions: {}

jobs:
  release:
    if: github.repository == 'butler54/sandboxctl'
    uses: butler54/sandboxctl-cicd/.github/workflows/python-release.yml@main
    secrets: inherit
